spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      data-source-properties:
        ssl: true
        sslmode: require
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
